/** How many recent user/assistant messages to send verbatim (not counting the current `message`). */
export const VERBATIM_MESSAGE_CAP = 10;

/** Max length for client-provided rolling summary (server trims). */
export const MAX_CONVERSATION_SUMMARY_CHARS = 12_000;
